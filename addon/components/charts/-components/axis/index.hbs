{{import onUpdate from '../../-helpers/on-update'}}
{{import toBool from '../../-helpers/to-bool'}}
{{import '{ fn }' from '@ember/helper'}}
{{import newObj from 'carbon-components-ember/helpers/new-obj'}}


{{#if @chart.setAxis}}
  {{onUpdate (fn @chart.setAxis @axis (newObj
    title=@title
    stacked=(toBool @stacked)
    scaleType=@scaleType
  )) @axis @title @stacked }}
{{/if}}

